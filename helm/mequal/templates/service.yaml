apiVersion: v1
kind: Service
metadata:
  name: {{ include "mequal.fullname" . }}
  labels:
    {{- include "mequal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mequal.selectorLabels" . | nindent 4 }}
